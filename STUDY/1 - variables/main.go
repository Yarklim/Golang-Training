package main

import "fmt"

func main() {
	text := "Hello, Yar!"  // Присвоение значения в переменную
	num := 111  // Присвоение значения в переменную

	num = num + 50  // Изменение значения в переменной

	fmt.Println(text)  // Вывод с переносом
	fmt.Print(num)  // Вывод в одну строку
}